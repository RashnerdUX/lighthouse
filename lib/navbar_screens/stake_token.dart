import 'package:flutter/material.dart';

class StakeScreen extends StatelessWidget {
  const StakeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Stake token'),
    );
  }
}
